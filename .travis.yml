language: python
python:
- "3.6"
branches:
  only:
    - travis
before_script:
- pip install -r requirements.txt
- sudo apt-get update
- sudo apt-get install -y spatialite-bin libspatialite3
- locate libspatialite3
- locate mod_spatialite
script: python build_cryptids_database.py
